CREATE TABLE STATUSVEHICLE(
ID INT IDENTITY PRIMARY KEY,
DESCR VARCHAR(50) NOT NULL
);

INSERT INTO STATUSVEHICLE(DESCR) VALUES ('Disponible')
INSERT INTO STATUSVEHICLE(DESCR) VALUES ('Mantenimiento')
INSERT INTO STATUSVEHICLE(DESCR) VALUES ('Fuera de Servicio')

CREATE TABLE VEHICLES(
ID INT IDENTITY PRIMARY KEY,
BRAND VARCHAR(50) NOT NULL,
MODEL VARCHAR(50) NOT NULL,
YEEAR INT NOT NULL,
PLATE VARCHAR(10) NOT NULL,
STATEID INT NOT NULL,
CONSTRAINT FK_STATUS_VEHICLE FOREIGN KEY(STATEID) REFERENCES STATUSVEHICLE(ID)
);

CREATE TABLE USERS (
	ID INT PRIMARY KEY IDENTITY(1,1),
	IDUSER VARCHAR(10) NOT NULL,
	PASSHASH VARBINARY(60) NOT NULL
);

INSERT INTO USERS(IDUSER,PASSHASH) VALUES ('TEST',CONVERT(VARBINARY(MAX), 'TESTPF2025$'))